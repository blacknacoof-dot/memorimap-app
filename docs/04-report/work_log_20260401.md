# Work Log 2026-04-01

## Summary

- Added Phase 0/1/2 onboarding flow for list-first entry.
- Hardened shared journey rate limit RLS and documented the live remediation.
- Tightened Vite dev exposure and removed unused `xlsx`.
- Fixed facility image fallback behavior so DB-assigned default images are respected.

## 1. Mobile/Desktop Onboarding

### Phase 0 Analytics

- Added GA4 events in `lib/analytics.ts`
  - `screen_view`
  - `first_interaction`
  - `tab_switch`
  - `cta_click`
  - `bounce`
- `bounce` is recorded only when 10 seconds pass with no click, scroll, or touch interaction.

### Phase 1 List-First Entry

- Added `config/featureFlags.ts` for rollback-safe control.
- Added `src/utils/device.ts` for viewport detection.
- Added list-first entry and list-screen messaging.
- Added first-interaction tracking for list card and bottom-nav interactions.

### Phase 2 Welcome Sheet

- Added `src/utils/onboarding.ts` with versioned localStorage key:
  - `memorimap_welcome_seen_v1`
- Added `components/WelcomeSheet.tsx`
- Connected CTA actions to:
  - list
  - sangjo comparison
  - existing SOS flow

### Final Entry Flow Adjustment

- Updated `App.tsx` so `mobileListDefault = true` now starts on `ViewState.LIST` for both mobile and desktop.
- Removed the viewport restriction from the welcome-sheet condition.

Result:

- First visit: `LIST + WelcomeSheet`
- Returning visit: `LIST only`

## 2. Facility Image Fallback Fix

Updated `utils/facilityNormalizer.ts`.

- Removed `'/defaults/'` from `BAD_URL_PATTERNS`
  - DB-assigned default image URLs should no longer be rejected as bad URLs.
- Replaced the old char-code sum hash with FNV-1a
  - deterministic output
  - more even fallback image distribution

Effect:

- DB-assigned default images are used as intended.
- Fallback selection is less collision-prone when no valid image exists.

## 3. Security / Infra

### Supabase RLS

- Added `supabase/migrations/20260401_fix_shared_journey_rate_limits_rls.sql`
- Added `docs/04-report/security_advisor_rls_remediation_20260401.md`
- Live remediation was applied for `public.shared_journey_rate_limits`
- Remaining advisor item: `public.spatial_ref_sys` is extension-owned and not alterable with project SQL permissions

### Vite / Package Cleanup

- Restricted Vite dev host exposure to localhost
- Removed `allowedHosts: true`
- Removed unused `xlsx`

## Feature Flags

| Flag | Value | Meaning |
|------|-------|---------|
| `mobileListDefault` | `true` | Start from list view |
| `mobileWelcomeSheet` | `true` | Show welcome sheet for first visit |
| `analytics` | `true` | Enable GA4 interaction tracking |

## Verification

- `npm run typecheck` passed
- `npm run build` passed
