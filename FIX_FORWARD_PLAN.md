# 구현 계획: Fix Forward (전진 수정) 전략

## 목표 및 배경
현재 프로젝트는 `App.tsx` 대규모 리팩토링(400줄 이하 달성)과 반려동물 예약 등 새로운 기능이 이미 구현된 상태입니다.
하지만 `types_schema.ts` 파일 손상과 일부 예약 기능의 런타임 오류가 보고되었습니다.
빌드(`tsc`) 자체는 성공적이므로, **롤백보다는 현재 상태에서 문제를 수정하여 진행하는 'Fix Forward' 전략을 강력히 권장**합니다.

## 사용자 검토 필요 사항
> [!IMPORTANT]
> **결정: 전진 수정 vs 롤백**
> - **권장**: **전진 수정 (Fix Forward)** - 리팩토링된 구조와 신규 기능을 유지하며 오류만 수정합니다.
> - **대안**: 롤백 (Rollback) - 리팩토링 이전 시점으로 되돌립니다. (모든 신규 작업 손실)
>
> 하단 계획은 '전진 수정'을 기준으로 작성되었습니다. 동의하시면 진행해 주세요.

## 제안된 변경 사항

### 1. 시스템 파일 복구
#### [재생성] `types_schema.ts`
- 현재 npm 로그가 잘못 저장되어 파일이 손상됨.
- 조치: `npm run types` 명령어로 올바른 스키마 파일 재생성.

### 2. 기능 오류 수정
#### [수정] AI / 일반 예약 (`ReservationModal.tsx`)
- `LegacyReservation` 타입 매핑 오류 수정.
- `user_id` 하드코딩('temp-user') 문제 해결 및 실제 사용자 ID 연동.
- 긴급 예약(`onUrgentSubmit`) 로직 검증.

#### [수정] 내 예약 조회 (`ReservationList.tsx` / 데이터 페칭)
- '내 예약' 목록이 보이지 않거나 에러가 발생하는 원인 파악.
- `hooks/useReservations.ts` 및 RLS(Row Level Security) 정책 확인.
- Supabase 권한 설정 점검.

## 검증 계획

### 자동화 테스트
- `npx tsc --noEmit` 실행으로 타입 에러 0건 확인.

### 수동 검증 시나리오
1.  **AI 예약 플로우**:
    - AI 채팅 시작 -> 시설 선택 -> 예약 진행.
    - 예약 완료 모달 확인 및 Supabase 데이터 저장 확인.
2.  **마이 페이지**:
    - 로그인 상태에서 마이 페이지 진입 -> '예약 내역' 탭 확인.
    - 리스트 렌더링 정상 여부 및 상세 보기 동작 확인.
