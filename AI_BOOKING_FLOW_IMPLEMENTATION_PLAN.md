# AI 예약 접수 플로우 통합 시나리오 & 구현 계획

## 1. 🎯 목표 (Objective)
시설 선택부터 예약 완료, 마이페이지 확인, 그리고 업체 대시보드 접수까지 이어지는 전체 예약 접수 시나리오를 통합하고 구현합니다.

---

## 2. 전체 유저 시나리오 (User Scenarios)

### 1단계: 시설 발견 (진입점)
사용자가 다음 진입점 중 하나를 통해 시설을 발견하고 [바로 예약 접수]를 클릭합니다.
*   **진입점 A**: AI 마음이 추천 시설 카드
*   **진입점 B**: 지도에서 시설 마커 클릭
*   **진입점 C**: 시설 목록에서 시설 클릭

### 2단계: 빈소 + 종교 선택 (BookingPreStep)
기존 상담 폼 진입 전, 필수 필수 정보인 빈소 규모와 종교를 먼저 선택합니다.
*   **빈소 규모**: 소규모 / 중규모 / 대규모 칩 선택
*   **종교**: 무교 / 기독교 / 천주교 / 불교 칩 선택

### 3단계: 빨간색 ConsultationForm (Urgent Mode)
인적사항 및 긴급 출동에 필요한 핵심 정보를 입력합니다.
*   **숨김 처리**: 2단계에서 수집된 종교/장지 섹션은 숨김 처리됩니다.
*   **입력 항목**: 고인 정보(성함, 성별, 위치), 신청인 정보, 운구 차량 필요 여부, 비상 연락망.

### 4단계: 예약 완료 확인
제출 즉시 예약 완료 화면을 표시합니다.
*   "바로 예약 되었습니다" 메시지 및 선택한 시설/조건 요약 표시.
*   [상담 내역 보기] 버튼을 통해 마이페이지로 이동 유도.

### 5단계: 마이페이지 (사용자 측)
*   사용자는 마이페이지 > 상담 탭에서 실시간으로 예약 상태(대기중/접수됨)를 확인합니다.
*   업체가 입력한 예상 도착 시간 및 안내 사항을 확인합니다.

### 6단계: 업체 대시보드 (시설 측)
*   시설 관리자는 대시보드에서 실시간 새 접수 알림을 받습니다.
*   [접수하기] 클릭 시 예상 도착 시간과 안내 사항을 입력하여 사용자에게 전달합니다.

---

## 3. 구현 계획 (Implementation Details)

### [MODIFY] components/Consultation/BookingPreStep.tsx [NEW]
*   시설명 표시 및 빈소/종교 칩 선택 UI 구현.
*   `onNext` 콜백을 통해 선택된 데이터를 상위 폼에 전달.

### [MODIFY] components/Consultation/BrandChatHelpers.tsx (ConsultationForm)
*   `preStepData` prop 추가 및 Section 4(종교/장지) 조건부 렌더링.
*   섹션 번호 동적 조정 (preStepData 존재 시 비상 연락망을 4번으로 표시).

### [MODIFY] AI 검색 폼 (Funeral/Memorial/PetSearchForm.tsx)
*   희망 지역 입력 필드 삭제 (시설 기반 예약이므로 불필요).
*   `BookingPreStep` -> `ConsultationForm` -> 완료 화면 흐름 제어 로직 구현.
*   `bookingComplete` 상태관리를 통한 결과 뷰 렌더링.

### [DATABASE] Supabase `consultations` 테이블
*   `scale`, `religion` 컬럼에 직접 데이터 저장 (현재 구현 완료).

---

## 4. 검증 계획 (Verification)
1.  **빌드 검증**: `npx tsc --noEmit` 통과 확인.
2.  **흐름 테스트**: 추천 시설 -> 바로 예약 -> 프리스텝 -> 상담 폼 -> 완료 화면 시퀀스 확인.
3.  **데이터 저장**: Supabase 테이블 내 데이터(scale, religion) 정상 저장 확인.
4.  **동기화 확인**: 마이페이지 및 업체 대시보드에서 접수 내역 정상 표출 확인.

---
**작성일**: 2025-02-13  
**상태**: 구현 완료 및 문서화 완료
