# Final Release Readiness Report (최종 릴리스 검증 보고서)

> **Date**: 2026-02-02
> **Status**: ✅ **Reviewed & Approved (Conditional)**
> **Context**: Final technical verification before release.

---

## 1️⃣ Overall Judgment (전체 판정)

| Metric | Status | Note |
| :--- | :--- | :--- |
| **Analysis Quality** | **High** | Risk identification is accurate. |
| **Risk Identification** | **Accurate** | Critical risks identified. |
| **Feasibility** | **High** | Can be executed immediately upon reflection. |
| **bkit Reliability** | **Normal** | - |

> [!IMPORTANT]
> **Conclusion**: 이 보고서는 실행 기준으로 충분히 정확합니다.
> 형식·논리·위험 식별 모두 실무 수준이며, **조건부 승인 판단은 정확**합니다.

---

## 2️⃣ Key Judgments Summary (핵심 판단 요약)

### ✅ Commendations (Most Critical Risks Avoided)

| # | Item | Impact | Verdict |
|---|------|--------|---------|
| 1 | **디렉토리 구조 불일치 지적** | 놓쳤으면 빌드 100% 실패 | `src` 가정 → **root 기반 수정** ✔️ |
| 2 | **`vite.config.ts` 덮어쓰기 차단** | `manualChunks` 덮으면 지도/벤더 분리 구조 붕괴 | `terser`만 병합 ✔️ |
| 3 | **`vercel.json` 병합 전략** | `framework`, `buildCommand`, `outputDirectory` 유지 필수 | `headers`만 추가 ✔️ |
| 4 | **DB 인덱스 중복 식별** | `CONCURRENTLY` 중복 생성 리스크 제거 | 정확 ✔️ |
| 5 | **롤백 전략** | 실제 복구 가능한 수준으로 상세함 | 충분 ✔️ |

> [!TIP]
> 이 4~5가지만으로도 보고서 가치는 충분함.

---

### ⚠️ Critical Supplements Required (반드시 보완 필요)

#### A. `nearby_facilities` 신규 RPC → **보류 필수**

> [!CAUTION]
> 현재 단계에서 추가하면 안 됨.

*   **선행 조건**: `search_facilities_v2` 성능 분석 필수
    ```sql
    EXPLAIN ANALYZE
    SELECT * FROM search_facilities_v2(...);
    ```
*   **Check**:
    *   GIST index 사용 여부
    *   Sequential scan 발생 여부
*   **결론**: `nearby_facilities` 신설은 **보류 판단이 맞음**.

---

#### B. Supabase Image Transform → **Feature Flag 처리**

> [!WARNING]
> Pro 플랜 전제는 운영 리스크.

*   **Context**:
    *   Memorimap: 업로드 빈도 낮음
    *   실시간 변환 필요성 낮음
*   **Recommendation**:
    *   `OptimizedImage` 컴포넌트는 유지 가능
    *   Transform 로직은 **옵션/플래그 처리** 권장
    ```typescript
    if (!SUPABASE_IMAGE_TRANSFORM_ENABLED) {
      return originalUrl;
    }
    ```

---

#### C. Alias 경로 확인 → **런타임 터지는 영역**

> [!IMPORTANT]
> "사소해 보여도 런타임에서 터지는" 영역. 실행 전 반드시 점검 필요.

*   **Action**: 아래 3곳의 일관성 검증 필수
    | File | Setting |
    |------|---------|
    | `tsconfig.json` | `paths: { "@/*": ["./*"] }` |
    | `vite.config.ts` | `resolve.alias: { '@': './' }` |
    | Import Statements | `@/lib/supabase` 등 |

*   **Risk**: 불일치 시 빌드 통과 → **런타임 에러** 발생

---

## 3️⃣ Execution Order Verification (DAY Plan)

> The **DAY 1 ~ DAY 4** sequence is rational and safe.

### Recommended Adjustments

**DAY 1**
*   **Add at end of Day 1**:
    ```sql
    -- Check index hit on actual usage query
    EXPLAIN ANALYZE
    SELECT ... FROM facilities WHERE ...;
    ```

**DAY 2**
*   **Constraint**: Do **NOT** replace all images with `OptimizedImage`.
*   **Scope**: Apply only to **List/Map Thumbnails**.

---

## 4️⃣ Final Go / No-Go Decision (최종 실행 판단)

| Decision | Status | Condition |
| :--- | :--- | :--- |
| **보고서 원문 그대로 실행** | ❌ **비권장** | - |
| **보고서의 수정안 반영 후 실행** | ⭕ **안전 (GO)** | 위 보완사항 적용 후 진행 |
| **`nearby_facilities` RPC 신규 추가** | ⏸️ **보류** | 현 시점 보류 |

---

## 5️⃣ Rollback Strategy (롤백 전략)

> [!NOTE]
> 현재 Git 상태가 Clean이므로 언제든 롤백 가능.

```bash
# 전체 롤백
git checkout .

# 특정 파일 롤백
git checkout -- <file_path>

# 커밋 후 롤백 (필요시)
git revert HEAD
```

---

## 6️⃣ Pre-Execution Checklist (실행 전 체크리스트)

- [ ] `tsconfig.json` / `vite.config.ts` alias 일치 확인
- [ ] `search_facilities_v2` EXPLAIN ANALYZE 실행
- [ ] Image Transform Feature Flag 추가 (optional)
- [ ] 롤백 포인트 커밋 생성
- [ ] Day 1 작업 시작

---

**Report Approved By**: User (2026-02-02)
**Report Generated By**: bkit PDCA Analysis
